pipeline {
    agent any
   
    stages{
        stage("checkout:git") {
            steps {
            git branch: "master", url: ""
        }

        stage("Build:Maven") { 
             steps {
             sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
